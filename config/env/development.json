{
    // log to stdout in dev env
    "access_log": "",
    "access_log_format": "dev",

    "imageable":
    {
        // allow all in dev env
        "whitelist": { "allowedHosts": [".*"] },

        // enable verbose logging
        "debug": true
    }
}